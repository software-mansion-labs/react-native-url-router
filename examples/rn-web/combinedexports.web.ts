import {
  BrowserRouter,
  Routes,
  Route as Route_,
  Navigate as Navigate_,
  useParams as useParams_,
  useNavigate as useNavigate_,
  Outlet as Outlet_,
  Link as Link_,
} from "react-router-dom";

export const StackOrRoutes = Routes;
export const TabOrRoutes = Routes;
export const PlatformSpecificRouter = BrowserRouter;
export const Route = Route_;
export const Navigate = Navigate_;
export const useParams = useParams_;
export const Link = Link_;
export const useNavigate = useNavigate_;
export const Outlet = Outlet_;
